package com.ma.lightweather.widget

import android.content.Context
import android.support.v7.widget.SearchView

class MySwipeRefreshLayout(context: Context) : SearchView(context) {
    init {
        initView()
    }

    private fun initView() {}
}
